import Card from '../../components/ui/Card'

/**
 * Orders page — STATUS: NOT YET IMPLEMENTED.
 * Scaffolded route only. Built out in a later phase per the incremental
 * plan (Master Prompt §33). See PHASE report for sequencing.
 */
export default function Orders() {
  return (
    <Card>
      <p className="text-sm text-soil-600">
        Orders module — scheduled for the next build phase.
      </p>
    </Card>
  )
}
